# 📘 راهنمای کاربردی کامل نحوه کارکرد سیستم ERP نسخه 8.0

## 🎯 مقدمه

این راهنما به شما نشان می‌دهد که چگونه سیستم ERP نسخه 8.0 را راه‌اندازی کرده و از آن برای مدیریت موجودی، تولید و مالی کسب‌وکار خود استفاده کنید. این سیستم برای صنایع غذایی، دارویی، لبنی و تولیدی طراحی شده و از استانداردهای جهانی SAP و Oracle پیروی می‌کند.

---

## 📋 بخش ۱: ساختار شیت‌های سیستم

سیستم از ۱۴ شیت تشکیل شده که به سه دسته تقسیم می‌شوند:

### ۱.۱ شیت‌های ورودی (داده‌های پایه)

| نام شیت | کاربرد | تعداد ستون‌ها |
|---------|--------|--------------|
| **ITEMS** | تعریف کالاها و ویژگی‌های آنها | ۸ ستون |
| **RECIPES** | فرمول ساخت محصولات (BOM) | ۵ ستون |
| **CONVERSIONS** | تبدیل واحدها | ۳ ستون |

### ۱.۲ شیت‌های تراکنش‌ها (عملیات روزانه)

| نام شیت | کاربرد | تعداد ستون‌ها |
|---------|--------|--------------|
| **PURCHASES** | ثبت خرید مواد اولیه | ۹ ستون |
| **PRODUCTION** | ثبت دستورات تولید | ۷ ستون |
| **SALES** | ثبت فروش محصولات | ۶ ستون |
| **WASTE** | ثبت ضایعات | ۷ ستون |
| **STOCK_TAKE** | ثبت انبارگردانی | ۷ ستون |
| **OPENING_BALANCES** | موجودی اولیه (فقط یک بار) | ۸ ستون |

### ۱.۳ شیت‌های سیستمی و گزارش‌ها

| نام شیت | کاربرد |
|---------|--------|
| **BOM_CACHE** | کش فرمول‌های ساخت (توسط سیستم پر می‌شود) |
| **INVENTORY_FINAL** | گزارش موجودی نهایی (سه‌بعدی) |
| **DAILY_DASHBOARD** | داشبورد روزانه |
| **SUSPENSE_ACCOUNT** | حساب تعلیقی (کسری‌ها) |
| **ERRORS_LOG** | لاگ خطاها و هشدارها |

---

## 🚀 بخش ۲: مراحل راه‌اندازی اولیه

### مرحله ۱: ایجاد محیط سیستم

**اقدام:** از منوی سیستم، گزینه `🛠 ایجاد/بازسازی تمام شیت‌های سیستم (نسخه ۸)` را اجرا کنید.

**نتیجه:** تمام ۱۴ شیت با هدرهای استاندارد ایجاد می‌شوند.

### مرحله ۲: تعریف واحدها و تبدیل‌ها (CONVERSIONS)

این شیت برای تبدیل واحدهای مختلف به یکدیگر استفاده می‌شود.

**ساختار ستون‌ها:**
```
fromUnit | toUnit   | factor
```

**مثال عملی:**
```
fromUnit    | toUnit    | factor
کیلوگرم     | گرم       | 1000
گرم         | کیلوگرم   | 0.001
لیتر        | میلی‌لیتر | 1000
میلی‌لیتر   | لیتر      | 0.001
جعبه        | عدد       | 24
عدد         | جعبه      | 0.04167
کارتن       | جعبه      | 12
```

**نکته مهم:** سیستم به صورت خودکار تبدیل معکوس را محاسبه می‌کند. اگر `کیلوگرم → گرم = 1000` را وارد کنید، سیستم خودکار `گرم → کیلوگرم = 0.001` را می‌سازد.

**تبدیل‌های غیرمستقیم:**
اگر تبدیل مستقیم نداشته باشید، سیستم از الگوریتم BFS برای یافتن مسیر استفاده می‌کند:
- مثال: `جعبه → کیلوگرم` (تبدیل مستقیم نداریم)
- مسیر: `جعبه → عدد (24) → کیلوگرم (0.5)` = ضریب نهایی: 12

### مرحله ۳: تعریف کالاها (ITEMS)

**ساختار ستون‌ها:**
```
itemCode | itemName | baseUnit | itemType | packageSize | parentItem | isCatchWeight | secondaryUnit
```

**توضیح هر ستون:**

| ستون | توضیح | مثال |
|------|-------|------|
| `itemCode` | کد یکتای کالا (اختیاری: عدد یا متن) | `ITEM_001` یا `1001` |
| `itemName` | نام فارسی کالا | `شیر خام پاستوریزه` |
| `baseUnit` | واحد پایه (همیشه در این واحد محاسبه می‌شود) | `لیتر` یا `کیلوگرم` |
| `itemType` | نوع کالا: `RAW` (ماده اولیه)، `PRODUCT` (محصول)، `PACKAGED` (بسته‌بندی) | `RAW` |
| `packageSize` | اندازه بسته (فقط برای PACKAGED) | `500` (گرم) |
| `parentItem` | کد کالای والد (فقط برای PACKAGED) | `ITEM_002` |
| `isCatchWeight` | آیا وزن متغیر دارد؟ `TRUE` یا `FALSE` | `TRUE` |
| `secondaryUnit` | واحد ثانویه (فقط برای Catch Weight) | `کیلوگرم` |

**مثال عملی - صنایع لبنی:**

```
itemCode  | itemName          | baseUnit | itemType   | packageSize | parentItem | isCatchWeight | secondaryUnit
ITEM_001  | شیر خام           | لیتر     | RAW        |             |            | FALSE         |
ITEM_002  | ماست فله          | کیلوگرم  | PRODUCT    |             |            | FALSE         |
ITEM_003  | ماست ۵۰۰ گرمی     | بسته    | PACKAGED   | 500         | ITEM_002   | FALSE         |
ITEM_004  | پنیر پیتزا        | کیلوگرم  | PRODUCT    |             |            | TRUE          | کیلوگرم
ITEM_005  | ظرف پلاستیکی ۵۰۰  | عدد      | RAW        |             |            | FALSE         |
ITEM_006  | درب ظرف           | عدد      | RAW        |             |            | FALSE         |
ITEM_007  | برچسب ماست        | عدد      | RAW        |             |            | FALSE         |
```

**نکات مهم:**
- `baseUnit` باید با واحدهای تعریف شده در `CONVERSIONS` مطابقت داشته باشد
- برای کالاهای `PACKAGED`، سیستم به صورت خودکار BOM تولید می‌کند
- `isCatchWeight` برای کالاهایی مثل گوشت، پنیر، میوه استفاده می‌شود

### مرحله ۴: تعریف فرمول‌های ساخت (RECIPES)

**ساختار ستون‌ها:**
```
menuCode | ingCode | qty | unit | yield
```

**توضیح هر ستون:**

| ستون | توضیح | مثال |
|------|-------|------|
| `menuCode` | کد محصول نهایی | `ITEM_002` (ماست فله) |
| `ingCode` | کد ماده اولیه | `ITEM_001` (شیر خام) |
| `qty` | مقدار مورد نیاز | `10` |
| `unit` | واحد مقدار | `لیتر` |
| `yield` | بازده درصدی (اختیاری، پیش‌فرض 100) | `95` |

**مثال عملی - تولید ماست:**

```
menuCode | ingCode  | qty  | unit    | yield
ITEM_002 | ITEM_001 | 10   | لیتر    | 95
ITEM_002 | ITEM_008 | 0.5  | کیلوگرم | 100
```

**تفسیر:** برای تولید ۱ کیلوگرم ماست فله:
- ۱۰ لیتر شیر خام (با بازده ۹۵٪)
- ۰.۵ کیلوگرم مایه ماست (با بازده ۱۰۰٪)

**BOM چندسطحی:**
اگر یک محصول از محصولات نیمه‌ساخته تشکیل شده باشد، سیستم به صورت خودکار BOM را بسط می‌دهد:

```
menuCode | ingCode  | qty | unit
ITEM_009 | ITEM_002 | 1   | کیلوگرم  (دستور پیتزا از ماست فله)
ITEM_002 | ITEM_001 | 10  | لیتر     (ماست فله از شیر)
```

سیستم به صورت خودکار محاسبه می‌کند که برای ۱ دستور پیتزا:
- ۱ کیلوگرم ماست فله
- ۱۰ لیتر شیر خام (از بسط BOM)

### مرحله ۵: به‌روزرسانی کش BOM

**اقدام:** از منوی سیستم، گزینه `⚙️ به‌روزرسانی کش فرمول ساخت (BOM)` را اجرا کنید.

**نتیجه:** شیت `BOM_CACHE` پر می‌شود و تمام فرمول‌های چندسطحی بسط داده می‌شوند.

**مثال خروجی BOM_CACHE:**
```
menuCode | ingCode  | qtyNeeded
ITEM_002 | ITEM_001 | 10.526    (۱۰ لیتر / ۰.۹۵ بازده)
ITEM_002 | ITEM_008 | 0.5
ITEM_003 | ITEM_002 | 0.5       (BOM خودکار بسته‌بندی)
```

### مرحله ۶: اعمال لیست کشویی

**اقدام:** از منوی سیستم، گزینه `🔄 اعمال لیست کشویی برای کدهای کالا` را اجرا کنید.

**نتیجه:** تمام ستون‌های `itemCode` و `menuCode` در شیت‌های تراکنش، لیست کشویی با فرمت `نام کالا | کد کالا` خواهند داشت.

---

## 💼 بخش ۳: عملیات روزانه

### ۳.۱ ثبت موجودی اولیه (OPENING_BALANCES)

**زمان استفاده:** فقط یک بار در ابتدای استفاده از سیستم یا ابتدای هر سال مالی

**ساختار ستون‌ها:**
```
itemCode | qty | wac | val | batchNumber | expiryDate | warehouseCode | catchWeight
```

**مثال عملی:**
```
itemCode  | qty  | wac    | val       | batchNumber | expiryDate | warehouseCode | catchWeight
ITEM_001  | 500  | 5000   | 2500000   | BATCH-001   | 1405/06/01 | WH_RAW        | 0
ITEM_002  | 100  | 50000  | 5000000   | BATCH-002   | 1405/03/15 | WH_PROD       | 0
ITEM_004  | 50   | 120000 | 6000000   | BATCH-003   | 1405/04/01 | WH_PROD       | 48.5
```

**توضیح:**
- `wac`: میانگین موزون قیمت (Weighted Average Cost)
- `val`: ارزش کل موجودی = `qty × wac`
- `batchNumber`: شماره بچ (اختیاری، اگر خالی باشد `OPENING_BATCH` در نظر گرفته می‌شود)
- `expiryDate`: تاریخ انقضا به فرمت جلالی (سیستم خودکار به میلادی تبدیل می‌کند)
- `warehouseCode`: کد انبار (مثلاً `WH_RAW` برای انبار مواد اولیه)
- `catchWeight`: وزن متغیر (فقط برای کالاهای Catch Weight)

### ۳.۲ ثبت خرید (PURCHASES)

**ساختار ستون‌ها:**
```
date | jalaliDate | itemCode | qty | unit | totalCost | batchNumber | expiryDate | warehouseCode | catchWeight
```

**مثال عملی - خرید شیر خام:**
```
date       | jalaliDate | itemCode  | qty  | unit  | totalCost | batchNumber | expiryDate | warehouseCode | catchWeight
1405/03/01 | 1405/03/01 | ITEM_001  | 1000 | لیتر  | 5000000   | BATCH-101   | 1405/06/01 | WH_RAW        | 0
```

**نکات مهم:**
- `date`: تاریخ میلادی (توسط سیستم از `jalaliDate` تولید می‌شود)
- `jalaliDate`: تاریخ جلالی (کاربر وارد می‌کند، فرمت: `1405/03/01`)
- `totalCost`: بهای تمام شده کل (نه قیمت واحد)
- `batchNumber`: شماره بچ منحصر به فرد (مثلاً `BATCH-101` یا `20260601-001`)
- `expiryDate`: تاریخ انقضا (بسیار مهم برای FEFO)
- `warehouseCode`: انبار مقصد (مثلاً `WH_RAW`)
- `catchWeight`: وزن واقعی (برای کالاهای وزن متغیر)

**مثال عملی - خرید پنیر با وزن متغیر:**
```
date       | jalaliDate | itemCode  | qty | unit   | totalCost | batchNumber | expiryDate | warehouseCode | catchWeight
1405/03/02 | 1405/03/02 | ITEM_004  | 40  | بسته  | 4800000   | BATCH-102   | 1405/04/01 | WH_PROD       | 19.8
```

**تفسیر:** ۴۰ بسته پنیر با وزن واقعی ۱۹.۸ کیلوگرم خریداری شده است.

### ۳.۳ ثبت دستور تولید (PRODUCTION)

**ساختار ستون‌ها:**
```
date | jalaliDate | menuCode | qtyProduced | batchNumber | expiryDate | warehouseCode | catchWeight
```

**مثال عملی - تولید ماست فله:**
```
date       | jalaliDate | menuCode  | qtyProduced | batchNumber | expiryDate | warehouseCode | catchWeight
1405/03/03 | 1405/03/03 | ITEM_002  | 90          | PROD-001    | 1405/03/13 | WH_PROD       | 0
```

**چه اتفاقی می‌افتد؟**

۱. **تولید محصول:**
   - ۹۰ کیلوگرم ماست فله با بچ `PROD-001` تولید می‌شود
   - تاریخ انقضا: ۱۴۰۵/۰۳/۱۳ (۱۰ روز بعد)
   - انبار مقصد: `WH_PROD`

۲. **مصرف مواد اولیه (به صورت خودکار):**
   - سیستم BOM را از `BOM_CACHE` می‌خواند
   - برای ۹۰ کیلوگرم ماست:
     - ۹۰ × ۱۰.۵۲۶ = ۹۴۷.۳۴ لیتر شیر خام مصرف می‌شود
     - ۹۰ × ۰.۵ = ۴۵ کیلوگرم مایه ماست مصرف می‌شود
   - سیستم به صورت خودکار از بچ‌های موجود (با FEFO) کسر می‌کند

۳. **محاسبه هزینه تولید:**
   - هزینه مواد مصرفی محاسبه می‌شود
   - WAC محصول تولید شده = هزینه کل / تعداد تولید

**مثال عملی - تولید ماست بسته‌بندی:**
```
date       | jalaliDate | menuCode  | qtyProduced | batchNumber | expiryDate | warehouseCode | catchWeight
1405/03/03 | 1405/03/03 | ITEM_003  | 180         | PROD-002    | 1405/03/13 | WH_PROD       | 0
```

**چه اتفاقی می‌افتد؟**

۱. **تولید محصول:**
   - ۱۸۰ بسته ماست ۵۰۰ گرمی با بچ `PROD-002` تولید می‌شود

۲. **مصرف مواد اولیه (BOM خودکار بسته‌بندی):**
   - سیستم به صورت خودکار BOM بسته‌بندی را تولید کرده است:
     - `ITEM_003 → ITEM_002 (0.5 کیلوگرم)`
   - برای ۱۸۰ بسته:
     - ۱۸۰ × ۰.۵ = ۹۰ کیلوگرم ماست فله مصرف می‌شود

**نکته مهم:** اگر `batchNumber` خالی بماند، سیستم به صورت خودکار بچ تولید می‌کند (مثلاً `PROD_1_1717315200000`).

### ۳.۴ ثبت فروش (SALES)

**ساختار ستون‌ها:**
```
date | jalaliDate | itemCode | qty | batchNumber | warehouseCode | catchWeight
```

**مثال عملی - فروش ماست:**
```
date       | jalaliDate | itemCode  | qty | batchNumber | warehouseCode | catchWeight
1405/03/04 | 1405/03/04 | ITEM_003  | 50  |             | WH_PROD       | 0
```

**چه اتفاقی می‌افتد؟ (الگوریتم FEFO)**

۱. **بررسی بچ مشخص شده:**
   - اگر `batchNumber` مشخص شده باشد (مثلاً `PROD-002`)، فقط از همان بچ کسر می‌شود
   - اگر خالی باشد یا `AUTO` باشد، سیستم FEFO اعمال می‌کند

۲. **اعمال FEFO:**
   - سیستم تمام بچ‌های موجود در `WH_PROD` برای `ITEM_003` را پیدا می‌کند
   - بچ‌ها را بر اساس `expiryDate` مرتب می‌کند (قدیمی‌ترین در اولویت)
   - به ترتیب از بچ‌ها کسر می‌کند تا مقدار فروش تأمین شود

۳. **محاسبه COGS:**
   - بهای تمام شده کالای فروش رفته = تعداد فروش × WAC بچ

۴. **مثال عملی FEFO:**
   ```
   بچ‌های موجود ITEM_003:
   - PROD-002: 100 بسته، انقضا: 1405/03/13
   - PROD-003: 80 بسته، انقضا: 1405/03/15
   - PROD-004: 50 بسته، انقضا: 1405/03/17
   
   فروش: 150 بسته
   
   سیستم به ترتیب کسر می‌کند:
   - از PROD-002: 100 بسته (تمام می‌شود)
   - از PROD-003: 50 بسته (30 بسته باقی می‌ماند)
   ```

**مثال عملی - فروش پنیر با وزن متغیر:**
```
date       | jalaliDate | itemCode  | qty | batchNumber | warehouseCode | catchWeight
1405/03/05 | 1405/03/05 | ITEM_004  | 10  |             | WH_PROD       | 4.95
```

**چه اتفاقی می‌افتد؟**
- ۱۰ بسته پنیر فروخته می‌شود
- وزن واقعی: ۴.۹۵ کیلوگرم
- سیستم به صورت تناسبی وزن را از بچ کسر می‌کند

### ۳.۵ ثبت ضایعات (WASTE)

**ساختار ستون‌ها:**
```
date | jalaliDate | itemCode | qty | unit | batchNumber | warehouseCode
```

**مثال عملی:**
```
date       | jalaliDate | itemCode  | qty | unit   | batchNumber | warehouseCode
1405/03/06 | 1405/03/06 | ITEM_001  | 50  | لیتر   | BATCH-101   | WH_RAW
```

**چه اتفاقی می‌افتد؟**
- ۵۰ لیتر شیر خام از بچ `BATCH-101` ضایع می‌شود
- ارزش ضایعات = ۵۰ × WAC بچ
- در داشبورد روزانه به عنوان `ارزش ضایعات` نمایش داده می‌شود

**نکته:** اگر `batchNumber` خالی باشد، سیستم FEFO اعمال می‌کند.

### ۳.۶ ثبت انبارگردانی (STOCK_TAKE)

**ساختار ستون‌ها:**
```
date | jalaliDate | itemCode | countedQty | unit | batchNumber | warehouseCode
```

**مثال عملی:**
```
date       | jalaliDate | itemCode  | countedQty | unit   | batchNumber | warehouseCode
1405/03/07 | 1405/03/07 | ITEM_001  | 480        | لیتر   | BATCH-101   | WH_RAW
```

**چه اتفاقی می‌افتد؟**

۱. **محاسبه تفاوت:**
   - موجودی سیستمی: ۵۰۰ لیتر
   - موجودی شمارش شده: ۴۸۰ لیتر
   - تفاوت: ۴۸۰ - ۵۰۰ = -۲۰ لیتر

۲. **اگر تفاوت مثبت باشد (کسری مثبت):**
   - موجودی افزایش می‌یابد
   - ارزش موجودی = تفاوت × WAC
   - WAC به‌روزرسانی می‌شود

۳. **اگر تفاوت منفی باشد (کسری منفی):**
   - موجودی کاهش می‌یابد
   - ارزش کسری = |تفاوت| × WAC
   - در `SUSPENSE_ACCOUNT` ثبت می‌شود

**نکته مهم:** اگر در یک روز چندین بار برای یک کالا انبارگردانی انجام شود، تمام مقادیر تجمیع می‌شوند.

---

## 🔄 بخش ۴: پردازش و گزارش‌گیری

### ۴.۱ اجرای موتور محاسباتی

**اقدام:** از منوی سیستم، گزینه `🚀 اجرای کامل محاسبات روزانه (FEFO & Multi-WH)` را اجرا کنید.

**چه اتفاقی می‌افتد؟ (مرحله به مرحله)**

**مرحله ۱: بارگذاری داده‌ها**
- تمام شیت‌های ورودی خوانده می‌شوند
- داده‌ها به آرایه‌ای از اشیاء تبدیل می‌شوند
- اعداد فارسی/عربی به عدد استاندارد تبدیل می‌شوند

**مرحله ۲: ساخت نقشه کالاها**
- تمام کالاها در یک `Map` ذخیره می‌شوند
- اطلاعات هر کالا: نام، واحد پایه، نوع، اندازه بسته، کالای والد، Catch Weight

**مرحله ۳: ساخت گراف تبدیل‌ها**
- تمام تبدیل‌ها در یک گراف جهت‌دار ذخیره می‌شوند
- تبدیل‌های معکوس به صورت خودکار اضافه می‌شوند

**مرحله ۴: تولید BOM خودکار**
- برای کالاهای `PACKAGED`، BOM به صورت خودکار تولید می‌شود
- به شیت `RECIPES` اضافه می‌شود

**مرحله ۵: ساخت دفتر کل یکپارچه (Ledger)**
- تمام تراکنش‌ها از شیت‌های مختلف خوانده می‌شوند
- به یک آرایه واحد تبدیل می‌شوند
- بر اساس تاریخ و نوع تراکنش مرتب می‌شوند

**مرحله ۶: پردازش تراکنش‌ها (موتور WAC و FEFO)**
- تراکنش‌ها به ترتیب پردازش می‌شوند
- موجودی به‌روزرسانی می‌شود
- WAC محاسبه می‌شود
- FEFO اعمال می‌شود
- COGS محاسبه می‌شود

**مرحله ۷: تولید گزارش‌ها**
- `INVENTORY_FINAL`: موجودی سه‌بعدی
- `DAILY_DASHBOARD`: شاخص‌های روزانه
- `SUSPENSE_ACCOUNT`: کسری‌ها
- `ERRORS_LOG`: خطاها

### ۴.۲ گزارش موجودی نهایی (INVENTORY_FINAL)

**ساختار ستون‌ها:**
```
انبار | کد کالا | نام کالا | شماره بچ | تاریخ انقضا | واحد | موجودی | وزن متغیر | WAC | ارزش دفتری
```

**مثال خروجی:**
```
انبار     | کد کالا  | نام کالا       | شماره بچ  | تاریخ انقضا | واحد   | موجودی | وزن متغیر | WAC    | ارزش دفتری
WH_RAW    | ITEM_001 | شیر خام        | BATCH-101 | 1405/06/01  | لیتر   | 500    | 0         | 5000   | 2500000
WH_PROD   | ITEM_002 | ماست فله       | PROD-001  | 1405/03/13  | کیلوگرم| 10     | 0         | 50000  | 500000
WH_PROD   | ITEM_003 | ماست ۵۰۰ گرمی  | PROD-002  | 1405/03/13  | بسته  | 130    | 0         | 25000  | 3250000
WH_PROD   | ITEM_004 | پنیر پیتزا     | BATCH-102 | 1405/04/01  | بسته  | 30     | 14.85     | 120000 | 3600000
```

**تفسیر:**
- هر ردیف نشان‌دهنده موجودی یک بچ خاص در یک انبار خاص است
- `وزن متغیر`: وزن واقعی باقی‌مانده (برای کالاهای Catch Weight)
- `WAC`: میانگین موزون قیمت برای این بچ خاص
- `ارزش دفتری`: ارزش کل موجودی این بچ

### ۴.۳ داشبورد روزانه (DAILY_DASHBOARD)

**ساختار ستون‌ها:**
```
تاریخ جلالی | خرید روز | بهای تمام شده فروش (COGS) | هزینه تولید روز | ارزش ضایعات
```

**مثال خروجی:**
```
تاریخ جلالی | خرید روز  | COGS      | هزینه تولید | ارزش ضایعات
1405/03/07  | 9800000   | 1250000   | 4500000     | 250000
```

**تفسیر:**
- `خرید روز`: مجموع بهای تمام شده خریدهای روز
- `COGS`: بهای تمام شده کالای فروش رفته
- `هزینه تولید`: هزینه مواد مصرفی در تولید روز
- `ارزش ضایعات`: ارزش کالاهای ضایع شده در روز

### ۴.۴ حساب تعلیقی (SUSPENSE_ACCOUNT)

**ساختار ستون‌ها:**
```
تاریخ جلالی | کد کالا | کسری موجودی موقت | نوع عملیات | ردیف منبع
```

**مثال خروجی:**
```
تاریخ جلالی | کد کالا  | کسری موجودی | نوع عملیات           | ردیف منبع
1405/03/04  | ITEM_003 | 20           | SALE                 | ردیف 5
1405/03/06  | ITEM_001 | 50           | PRODUCTION_CONSUME   | ردیف 3
```

**تفسیر:**
- وقتی موجودی کافی نباشد، تراکنش در حساب تعلیقی ثبت می‌شود
- باید بررسی شود که آیا داده‌ها درست وارد شده‌اند یا خیر
- ممکن است نشان‌دهنده سرقت، اشتباه در ثبت، یا تأخیر در ثبت خرید باشد

### ۴.۵ لاگ خطاها (ERRORS_LOG)

**انواع خطاها:**

۱. **خطای تبدیل واحد:**
   ```
   [هشدار تبدیل] واحد 'جعبه' به 'گرم' برای ITEM_001 یافت نشد. مقدار بدون تبدیل ثبت شد.
   ```

۲. **خطای BOM:**
   ```
   [BOM Error] تبدیل واحد 'لیتر' به 'کیلوگرم' برای ITEM_001 یافت نشد.
   ```

۳. **خطای تولید:**
   ```
   [هشدار تولید] فرمول ساخت برای ITEM_003 یافت نشد.
   ```

۴. **خطای والد:**
   ```
   [خطا] کالای والد ITEM_999 برای ITEM_003 یافت نشد.
   ```

---

## 🎯 بخش ۵: مثال کامل عملی

### سناریو: کارخانه تولید ماست

**مرحله ۱: تعریف داده‌های پایه**

ITEMS:
```
itemCode  | itemName          | baseUnit | itemType   | packageSize | parentItem | isCatchWeight | secondaryUnit
ITEM_001  | شیر خام           | لیتر     | RAW        |             |            | FALSE         |
ITEM_002  | ماست فله          | کیلوگرم  | PRODUCT    |             |            | FALSE         |
ITEM_003  | ماست ۵۰۰ گرمی     | بسته    | PACKAGED   | 500         | ITEM_002   | FALSE         |
ITEM_005  | ظرف پلاستیکی      | عدد      | RAW        |             |            | FALSE         |
ITEM_006  | درب ظرف           | عدد      | RAW        |             |            | FALSE         |
ITEM_007  | برچسب ماست        | عدد      | RAW        |             |            | FALSE         |
```

CONVERSIONS:
```
fromUnit | toUnit   | factor
کیلوگرم  | گرم      | 1000
گرم      | کیلوگرم  | 0.001
```

RECIPES:
```
menuCode | ingCode  | qty  | unit    | yield
ITEM_002 | ITEM_001 | 10   | لیتر    | 95
ITEM_002 | ITEM_008 | 0.5  | کیلوگرم | 100
ITEM_003 | ITEM_005 | 1    | عدد     | 100
ITEM_003 | ITEM_006 | 1    | عدد     | 100
ITEM_003 | ITEM_007 | 1    | عدد     | 100
```

**مرحله ۲: اجرای setupEnvironment و updateBOMCache**

BOM_CACHE (خروجی):
```
menuCode | ingCode  | qtyNeeded
ITEM_002 | ITEM_001 | 10.526
ITEM_002 | ITEM_008 | 0.5
ITEM_003 | ITEM_002 | 0.5        (BOM خودکار بسته‌بندی)
ITEM_003 | ITEM_005 | 1
ITEM_003 | ITEM_006 | 1
ITEM_003 | ITEM_007 | 1
```

**مرحله ۳: ثبت موجودی اولیه**

OPENING_BALANCES:
```
itemCode  | qty   | wac    | val       | batchNumber | expiryDate | warehouseCode
ITEM_001  | 1000  | 5000   | 5000000   | BATCH-001   | 1405/06/01 | WH_RAW
ITEM_005  | 10000 | 500    | 5000000   | BATCH-002   |            | WH_RAW
ITEM_006  | 10000 | 200    | 2000000   | BATCH-003   |            | WH_RAW
ITEM_007  | 10000 | 100    | 1000000   | BATCH-004   |            | WH_RAW
```

**مرحله ۴: ثبت خرید**

PURCHASES:
```
date       | jalaliDate | itemCode  | qty  | unit    | totalCost | batchNumber | expiryDate | warehouseCode
1405/03/01 | 1405/03/01 | ITEM_008  | 100  | کیلوگرم | 2000000   | BATCH-101   | 1405/04/01 | WH_RAW
```

**مرحله ۵: ثبت تولید**

PRODUCTION:
```
date       | jalaliDate | menuCode  | qtyProduced | batchNumber | expiryDate | warehouseCode
1405/03/02 | 1405/03/02 | ITEM_002  | 90          | PROD-001    | 1405/03/12 | WH_PROD
1405/03/02 | 1405/03/02 | ITEM_003  | 180         | PROD-002    | 1405/03/12 | WH_PROD
```

**پردازش تولید ITEM_002 (ماست فله):**
- مصرف: 90 × 10.526 = 947.34 لیتر شیر از BATCH-001
- مصرف: 90 × 0.5 = 45 کیلوگرم مایه ماست از BATCH-101
- تولید: 90 کیلوگرم ماست فله با بچ PROD-001
- هزینه مواد: (947.34 × 5000) + (45 × 20000) = 4,736,700 + 900,000 = 5,636,700 تومان
- WAC ماست فله: 5,636,700 / 90 = 62,630 تومان/کیلوگرم

**پردازش تولید ITEM_003 (ماست بسته‌بندی):**
- مصرف: 180 × 0.5 = 90 کیلوگرم ماست فله از PROD-001
- مصرف: 180 × 1 = 180 عدد ظرف
- مصرف: 180 × 1 = 180 عدد درب
- مصرف: 180 × 1 = 180 عدد برچسب
- تولید: 180 بسته ماست ۵۰۰ گرمی با بچ PROD-002
- هزینه مواد: (90 × 62,630) + (180 × 500) + (180 × 200) + (180 × 100) = 5,636,700 + 90,000 + 36,000 + 18,000 = 5,780,700 تومان
- WAC ماست بسته‌بندی: 5,780,700 / 180 = 32,115 تومان/بسته

**مرحله ۶: ثبت فروش**

SALES:
```
date       | jalaliDate | itemCode  | qty | batchNumber | warehouseCode
1405/03/03 | 1405/03/03 | ITEM_003  | 50  |             | WH_PROD
1405/03/04 | 1405/03/04 | ITEM_003  | 30  |             | WH_PROD
```

**پردازش فروش:**
- فروش ۵۰ بسته: COGS = 50 × 32,115 = 1,605,750 تومان
- فروش ۳۰ بسته: COGS = 30 × 32,115 = 963,450 تومان
- موجودی باقی‌مانده: 180 - 50 - 30 = 100 بسته

**مرحله ۷: اجرای runFinancialEngine**

**INVENTORY_FINAL (خروجی):**
```
انبار     | کد کالا  | نام کالا       | شماره بچ  | تاریخ انقضا | واحد   | موجودی | WAC     | ارزش دفتری
WH_RAW    | ITEM_001 | شیر خام        | BATCH-001 | 1405/06/01  | لیتر   | 52.66  | 5000    | 263300
WH_RAW    | ITEM_005 | ظرف پلاستیکی   | BATCH-002 |             | عدد    | 9820   | 500     | 4910000
WH_RAW    | ITEM_006 | درب ظرف        | BATCH-003 |             | عدد    | 9820   | 200     | 1964000
WH_RAW    | ITEM_007 | برچسب ماست     | BATCH-004 |             | عدد    | 9820   | 100     | 982000
WH_RAW    | ITEM_008 | مایه ماست      | BATCH-101 | 1405/04/01  | کیلوگرم| 55     | 20000   | 1100000
WH_PROD   | ITEM_003 | ماست ۵۰۰ گرمی  | PROD-002  | 1405/03/12  | بسته  | 100    | 32115   | 3211500
```

**DAILY_DASHBOARD (خروجی):**
```
تاریخ جلالی | خرید روز | COGS      | هزینه تولید | ارزش ضایعات
1405/03/04  | 2000000  | 2569200   | 11417400    | 0
```

**تفسیر:**
- خرید روز: ۲,۰۰۰,۰۰۰ تومان (مایه ماست)
- COGS: ۲,۵۶۹,۲۰۰ تومان (فروش ۸۰ بسته ماست)
- هزینه تولید: ۱۱,۴۱۷,۴۰۰ تومان (تولید ماست فله و بسته‌بندی)
- ارزش ضایعات: ۰

---

## ⚠️ بخش ۶: نکات مهم و عیب‌یابی

### ۶.۱ نکات مهم

۱. **ترتیب ثبت تراکنش‌ها:**
   - تاریخ‌ها باید به ترتیب صعودی باشند
   - در یک روز، ابتدا خرید، سپس تولید، سپس فروش ثبت شود

۲. **فرمت تاریخ جلالی:**
   - فرمت استاندارد: `1405/03/01`
   - سیستم به صورت خودکار به میلادی تبدیل می‌کند
   - از کاراکترهای فارسی/عربی استفاده نکنید

۳. **شماره بچ:**
   - باید منحصر به فرد باشد
   - پیشنهاد: `BATCH-YYYYMMDD-XXX` یا `PROD-XXX`
   - اگر خالی بماند، سیستم خودکار تولید می‌کند

۴. **تاریخ انقضا:**
   - برای کالاهای فاسدشدنی الزامی است
   - سیستم FEFO بر اساس این تاریخ کار می‌کند
   - فرمت جلالی وارد کنید

۵. **کد انبار:**
   - باید ثابت باشد (مثلاً `WH_RAW`, `WH_PROD`)
   - اگر خالی بماند، `DEFAULT_WH` در نظر گرفته می‌شود

۶. **وزن متغیر:**
   - فقط برای کالاهایی که `isCatchWeight = TRUE` استفاده شود
   - هنگام خرید و فروش، وزن واقعی وارد شود

### ۶.۲ عیب‌یابی

**مشکل ۱: کش BOM خالی است**
```
خطا: کش BOM خالی است اما دستور تولید وجود دارد.
```
**راه‌حل:** گزینه `⚙️ به‌روزرسانی کش فرمول ساخت (BOM)` را اجرا کنید.

**مشکل ۲: تبدیل واحد یافت نشد**
```
هشدار: تبدیل 'جعبه' به 'گرم' یافت نشد.
```
**راه‌حل:** در شیت `CONVERSIONS` تبدیل‌های لازم را اضافه کنید.

**مشکل ۳: کسری موجودی**
```
حساب تعلیقی: ITEM_003 - 20 بسته
```
**راه‌حل:** بررسی کنید که آیا خریدها درست ثبت شده‌اند یا خیر.

**مشکل ۴: فرمول ساخت یافت نشد**
```
هشدار: فرمول ساخت برای ITEM_003 یافت نشد.
```
**راه‌حل:** در شیت `RECIPES` فرمول ساخت را اضافه کنید.

**مشکل ۵: تاریخ نامعتبر**
```
خطا: فرمت تاریخ جلالی نامعتبر: 1405-3-15
```
**راه‌حل:** از فرمت استاندارد `1405/03/15` استفاده کنید.

---

## 🎓 بخش ۷: بهترین روش‌ها

### ۷.۱ نام‌گذاری استاندارد

**کدهای کالا:**
- مواد اولیه: `RAW_XXX` یا `ITEM_001`
- محصولات: `PROD_XXX` یا `ITEM_100`
- بسته‌بندی: `PKG_XXX` یا `ITEM_200`

**شماره بچ‌ها:**
- خرید: `BATCH-YYYYMMDD-XXX` (مثلاً `BATCH-20260601-001`)
- تولید: `PROD-YYYYMMDD-XXX` (مثلاً `PROD-20260602-001`)

**کدهای انبار:**
- انبار مواد اولیه: `WH_RAW`
- انبار محصول: `WH_PROD`
- انبار ضایعات: `WH_WASTE`
- انبار موقت: `WH_TEMP`

### ۷.۲ گردش کار روزانه

**صبح:**
۱. بررسی موجودی روز قبل در `INVENTORY_FINAL`
۲. بررسی کسری‌ها در `SUSPENSE_ACCOUNT`
۳. بررسی خطاها در `ERRORS_LOG`

**در طول روز:**
۱. ثبت خریدها در `PURCHASES`
۲. ثبت دستورات تولید در `PRODUCTION`
۳. ثبت فروش‌ها در `SALES`
۴. ثبت ضایعات در `WASTE`

**عصر:**
۱. اجرای `runFinancialEngine`
۲. بررسی گزارش‌های روزانه
۳. تهیه گزارش برای مدیریت

**پایان ماه:**
۱. انجام انبارگردانی
۲. ثبت در `STOCK_TAKE`
۳. اجرای مجدد `runFinancialEngine`
۴. بررسی و تأیید موجودی

### ۷.۳ نکات امنیتی

۱. **پشتیبان‌گیری:**
   - هر روز از فایل Google Sheets نسخه پشتیبان تهیه کنید
   - از `File → Download → Microsoft Excel` استفاده کنید

۲. **دسترسی‌ها:**
   - فقط کاربران مجاز دسترسی ویرایش داشته باشند
   - از `Share → Restrict access` استفاده کنید

۳. **بررسی خطاها:**
   - هر روز `ERRORS_LOG` را بررسی کنید
   - کسری‌ها را در `SUSPENSE_ACCOUNT` پیگیری کنید

---

## 📊 بخش ۸: گزارش‌های پیشرفته

### ۸.۱ گزارش موجودی بر اساس تاریخ انقضا

برای تهیه گزارش موجودی نزدیک به انقضا:
۱. شیت `INVENTORY_FINAL` را باز کنید
۲. ستون `تاریخ انقضا` را مرتب کنید
۳. کالاهای با تاریخ انقضای نزدیک را شناسایی کنید

### ۸.۲ گزارش ارزش موجودی بر اساس انبار

برای تهیه گزارش ارزش موجودی هر انبار:
۱. شیت `INVENTORY_FINAL` را باز کنید
۲. از `Data → Pivot table` استفاده کنید
۳. ردیف‌ها: `انبار`
۴. مقادیر: `SUM(ارزش دفتری)`

### ۸.۳ گزارش COGS بر اساس محصول

برای تهیه گزارش بهای تمام شده هر محصول:
۱. شیت `SALES` را باز کنید
۲. از `Data → Pivot table` استفاده کنید
۳. ردیف‌ها: `itemCode`
۴. مقادیر: `SUM(qty)`

---

## 🎯 نتیجه‌گیری

سیستم ERP نسخه 8.0 یک سیستم جامع و حرفه‌ای برای مدیریت موجودی، تولید و مالی است. با پیروی از این راهنما، می‌توانید:

✅ سیستم را به درستی راه‌اندازی کنید  
✅ عملیات روزانه را به صورت استاندارد انجام دهید  
✅ از قابلیت‌های پیشرفته مانند FEFO و Catch Weight استفاده کنید  
✅ گزارش‌های دقیق و قابل اعتماد تهیه کنید  
✅ خطاها را شناسایی و رفع کنید  

این سیستم منطبق بر استانداردهای جهانی SAP MM/PP و Oracle است و برای صنایع غذایی، دارویی، لبنی و تولیدی مناسب است.

اگر سؤال یا مشکلی دارید، بخش `ERRORS_LOG` را بررسی کنید و در صورت نیاز با پشتیبانی تماس بگیرید.